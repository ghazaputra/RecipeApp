<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ghaza.idn.resepapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/food"
        android:label="@string/app_name"
        android:roundIcon="@drawable/food"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResepApp">
        <activity
            android:name="ghaza.idn.resepapp.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.MyTheme">
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ghaza.idn.resepapp.Profile" />
        <activity
            android:name="ghaza.idn.resepapp.DetailActivity"
            android:exported="true" />
        <activity
            android:name="ghaza.idn.resepapp.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>